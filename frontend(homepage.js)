import React from 'react';
import { useNavigate } from 'react-router-dom';

const HomePage = () => {
  const navigate = useNavigate();

  return (
    <div>
      <h1>Welcome to Telemedicine Platform</h1>

      <button onClick={() => navigate('/video-call')}>Start Video Call</button>
      <button onClick={() => navigate('/chat')}>Chat with Doctor</button>
      <button onClick={() => navigate('/appointments')}>Book Appointment</button>
    </div>
  );
};

export default HomePage;
